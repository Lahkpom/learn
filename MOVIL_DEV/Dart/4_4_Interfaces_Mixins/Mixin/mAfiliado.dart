mixin mAfiliado{
  //*Esto es un mixin, son un tipo de herencia múltiple que nos permite obtener los métodos de una clase sin nesecidad de instanciarla
  //*Los métodos pueden tener body
  void mostrarAfiliacion(String name) => print('El usuario ${name} está afiliado');
}